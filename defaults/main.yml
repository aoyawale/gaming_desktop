---
# defaults file for gaming_desktop

gaming:
   apps:
     - steam
     - vlc
     - obs-studio
     - clementine
     - wine
     - dolphin-emu
   pip:
     - virtualenv
  ruby_gems:
      - chef
      - rbenv
  virtualenvs:
  - venv1:
      path: /root/venv1
      packages:
        - ansible
        - requests
  - venv2
      path: /home/mike/venv2
      packages:
         - fabric

